<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.dgit.library_project.dao.BookDao">
	<sql id="selectSql">
		select * from book
	</sql>
	
	<sql id="insertSql">
		insert into book (book_code, book_name, author, public_name, price, amount) 
			values(#{bookCode}, #{bookName}, #{author}, #{publicName}, #{price}, #{amount})
	</sql>
	
	<sql id="updateSql">
		update book set book_name=#{bookName}, author=#{author}, public_name=#{publicName}, price=#{price}, amount=#{amount}	where book_code=#{bookCode}
	</sql>
	
	<sql id="deleteSql">
		delete from book 
	</sql>
	
	<sql id="whereSqlCode">
		<where>
			<if test="bookCode != null">
				where book_code=#{bookCode}
			</if>
		</where>
	</sql>
	
	<sql id="whereSqlAll">
		<where>
			<if test="bookCode != null">
				book_code=#{bookCode}
			</if>
			<if test="bookName != null">
				and book_name=#{bookName}
			</if>
			<if test="author != null">
				and author=#{author}
			</if>
			<if test="publicName != null">
				and public_name=#{publicName}
			</if>
		</where>
	</sql>
	
	<!-- selectBookByAll -->
	<select id="selectBookByAll" parameterType="Book" resultType="Book">
		<include refid="selectSql"/>
	</select>
	
	<!-- selectBookBySomething -->
	<select id="selectBookBySomething" parameterType="Book" resultType="Book">
		<include refid="selectSql"/>
		<include refid="whereSqlAll"/>
	</select>

	<!-- insertBook -->
	<insert id="insertBook">
		<include refid="insertSql"/>
	</insert>
	
	<!-- updateBook -->
	<update id="updateBook" parameterType="Book">
		<include refid="updateSql"/>
	</update>
	
	<!-- deleteBook -->
	<delete id="deleteBook">
		<include refid="deleteSql"/>
		<include refid="whereSqlAll"/>
	</delete>

</mapper>